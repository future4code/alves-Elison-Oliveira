```
 calculaSalario(qtdeCarrosVendidos, valorTotalVendas) {
 // Escreva seu código aqui

const totalDeVendas = valorTotalVendas * 5 / 100
const comisao = qtdeCarrosVendidos * 100
const salarioTotal = totalDeVendas + comisao + 2000
return salarioTotal
}
```

```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
 if (quantidade<12){
   return quantidade * 1.30
 }else{
   return quantidade
 }

}
```
